import { createAction } from '@reduxjs/toolkit';

export const toggleShowEmployeeInfo = createAction('toggleShowEmployeeInfo');
export const changeCurrentEmployee = createAction('changeCurrentEmployee');
